# PRD: 登录 / 落地（`/login` `/auth/*` `/`）

> 见 [README](./README.md)。本轮只定产品功能范围，不含实现。新功能改本文件。

## 目的
host 登录入口；落地页引导。**访客永不登录**（凭链接/二维码留位）。

## 当前功能（已建）
- 登录：magic link / 6 位 OTP / Google；跨标签会话检测；重发冷却；错误页。
- 落地页 `/`：营销首屏 + 极光动效 + CTA（建局/浏览）+ 语言切换。

## MVP 需要的功能
- **无成局新增**：维持现状。落地页保留建局/浏览 CTA。

## 缓做 / 不做（cluster A 缓做）
首页 `/` 直接做成局卡流 · 城市选择器（§10）· onboarding 完形填空调频（§9）。

## 依赖的成局契约
无（支撑页）。

## 品牌基调约束
待 PDF：落地页主张、语气、视觉调性。

## 状态
功能范围已定（Step 10A）。MVP 维持现状。
